/* eslint-disable no-shadow */
export enum ThemeModes {
	LIGHT = 'LIGHT',
	DARK = 'DARK'
}

export enum TemperatureUnit {
	FAR = '° F',
	CEL = '° C',
	KEL = '° K'
}

export enum ForecastMode {
	DAYS = 'DAYS',
	HOUR = 'HOUR'
}
